import 'package:flutter/material.dart';

class SubscriptionExpirationScreen extends StatelessWidget {
  const SubscriptionExpirationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SubscriptionExpirationScreen'),
    );
  }
}
